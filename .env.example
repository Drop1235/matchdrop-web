# League webhook and OP export API
# Configure these in Netlify site environment variables or local dev env
LEAGUE_WEBHOOK_URL=https://example.com/api/op/result
OP_WEBHOOK_SECRET=replace-with-shared-hmac-secret
# Optional: required for GET /api/export/results when set
OP_API_KEY=optional-api-key
